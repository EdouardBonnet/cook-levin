This annotated companion follows the concept definitions and summarizes the checked Lean proofs. All 28 passages start inside their statement or proof paragraph, after the heading; proof passages end after the QED marker. Only the main result is a theorem.

The paper uses the unchanged binary encodings and machine-class definitions from [lax-434930](https://laxarchive.org/lax-434930/).

The plain and annotated PDFs each have three pages with identical body text (1267 words). All 28 annotation starts are inside their matching reflow paragraphs. Targets resolve against the concept and proof identifiers. Direct TeX compilation was used because local latexmk 4.76 predates the CLI requirement of 4.77.
